20260909
줄 삭제 애니메이션 - 블록이 칸마다 에쿠보로 변하면서 승천하게 할 거임
- lockPiece() (블록 고정) 다음, clearRow() 호출 직전에 스냅숏 뜨기 - 지워지기 전의 그리드 상태
- lineClearAnimation 객체에 담을 것: 지워진 row 인덱스, grid 스냅숏, 애니메이션 시작 시각, duration, row별 삭제 패턴과 순서를 배열로 만들기
- draw()는 lineClearAnimation이 있으면 스냅숏 기준으로 그리고 사라지는 칸은 col별 delay만큼 늦게 애니메이션 동작(에쿠보 얼굴이 위로 페이드아웃하고 위쪽 칸은 새 위치로 이동), 없으면 원래대로 그림
- 애니메이션 동작 시간(duration)이 넘으면 lineClearAnimation = null로 되돌림

줄 지워질 때 패턴 결정 로직
- 지워진 row가 1개: 왼->오 / 오->왼 / 뒤죽박죽 중 랜덤으로 하나
- 지워진 row가 2개 이상: 왼->오 / 오->왼 중 시작 패턴만 랜덤으로 뽑고, row 인덱스 순서대로 번갈아 적용 (i % 2로 뒤집기)


20260908
미리보기 블록 가운데 정렬
- 각 블록의 행렬의 최댓값에서 최솟값 빼서 길이와 너비 구하기 V
- 미리보기 상자의 크기에서 블록의 길이와 너비 빼기 V
- 빼고 남은 미리보기 상자의 길이와 너비를 반으로 나누기
- 반으로 나눠서 구한 행과 열의 offset 값을 미리보기에 뜨는 블록의 위치값에 더해주기
- 미리보기랑 보드의 블록을 따로 그려줘야 될 듯


start 화면에서 스페이스바랑 esc가 하드드롭과 일시정지로 바로 이어지는 거 고치기

게임오버 화면에서 press SPACE BAR to restart 안 뜨는 거 고치기


20260907
게임 오버에서 스타트로 화면 전환 - 게임 상태도 초기화해야 함

게임 restart 할 때 초기화할 변수
- main.js
score
level
finalScore
endingLevel
gameoverEnteredTime
timePreviousFrame
dropTimeCounter
dropInterval
clearedRowsCounter
isSoftDropping

- piece.js
currentPiece

- board.js
grid

- state.js
nieckname

20260903
줄 삭제 애니메이션과 관계 없이 게임이 진행됐으면 좋겠음.
상상한 애니메이션: 삭제될 줄이 옆에서부터 칸이 하나씩 사라지고 위 칸들이 부드럽게 내려오기

애니메이션 진행에 필요한 정보
- 삭제 실행 직전 grid 스냅숏 (안전하게 row 단위로 깊이 복사)
- 삭제할 rows의 인덱스
- 삭제 애니메이션 시작 시각
- 삭제 애니메이션에 걸릴 시간
- 삭제 애니메이션 종료 후 부드럽게 내려올 위의 rows


<점수 매기기>
점수 매기기가 더 급해서 점수 매기기부터 함
근데 시간 관련해서 두 가지가 얽혀 있는 걸 발견해서 
(dropTimeUpdate는 자동 낙하시키는 거였고 ArrowDown 눌렀을 때 moveDown()을 setInterval로 호출한 건 꾹 눌렀을 때 브라우저마다 다르게 반복실행하니까 그걸 setInterval로 시간 간격을 정해주려는 거)
정리하려고 함

function dropTimeUpdate
time: requestAnimationFrame이 넘겨주는 값(브라우저 페이지가 로드된 시점(navigation 시작 시점)부터 지금까지 지난 시간(ms))
deltaTime: 직전 프레임 이후 지난 시간(프레임 시간 차이)
timePreviousFrame: 직전 프레임의 시각
dropTimeCounter: 누적 deltaTime (dropInterval 시간이 차면 초기화됨)


[js 파일]
- state.js: 화면 전환 관리 (START, PLAYING, PAUSED, GAMEOVER)
- board.js: 게임이 진행되는 보드 관리. grid 생성.
- piece.js: 블록 관리. pieces 모양, 랜덤 블록 생성, 좌표 계산, 큐에 쌓기, 스폰하기
- renderer.js: 그리기. drawCell(한 칸 칠하는 기능), drawGrid(보드 배열 중 차 있는 칸 있을 때 drawCell 호출), drawPiece(블록 배열 중 차 있는 칸 있을 때 drawCell 호출)
- main.js: 전체 기능 연결